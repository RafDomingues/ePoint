import { StyleSheet } from "react-native"

/**
 * Main style component
 */

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  floatingButton: {
    position: 'absolute',
    right: 10,
    bottom: 10
  }
})
